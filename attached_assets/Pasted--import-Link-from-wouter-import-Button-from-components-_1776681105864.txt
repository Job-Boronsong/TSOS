
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { ArrowRight, ChevronRight, CheckCircle2 } from "lucide-react";
import { SiPython, SiDjango, SiJavascript, SiPostgresql, SiGithub, SiReact, SiFlask } from "react-icons/si";
import { Cloud, Globe, Shield } from "lucide-react";

// ✅ FIXED IMAGE IMPORTS (relative paths)
import heroImg from "../assets/images/hero-tech.png";
import service1 from "../assets/images/service-custom.png";
import service2 from "../assets/images/service-backend.png";
import service3 from "../assets/images/service-api.png";
import service4 from "../assets/images/service-maintenance.png";
import product1 from "../assets/images/product-agribusiness.png";
import product2 from "../assets/images/product-school.png";

export default function Home() {
  return (
    <div className="flex flex-col min-h-screen">

      {/* Hero Section */}
      <section className="relative pt-32 pb-20 md:pt-48 md:pb-32 overflow-hidden">
        <div className="container mx-auto px-4 md:px-6 grid lg:grid-cols-2 gap-12 items-center">

          <div>
            <h1 className="text-5xl md:text-6xl font-bold mb-6">
              We build systems that <span className="text-primary">scale</span>.
            </h1>
            <p className="text-lg text-muted-foreground mb-8">
              Torrential Technologies engineers secure, reliable digital infrastructure.
            </p>

            <div className="flex gap-4">
              <Button asChild>
                <Link href="/contact">Discuss Your Project</Link>
              </Button>
              <Button variant="outline" asChild>
                <Link href="/services">Explore Services</Link>
              </Button>
            </div>
          </div>

          <div className="rounded-2xl overflow-hidden shadow-2xl">
            <img src={heroImg} alt="Hero" className="w-full h-full object-cover" />
          </div>

        </div>
      </section>

      {/* Services */}
      <section className="py-24">
        <div className="container mx-auto px-4">
          <h2 className="text-3xl font-bold mb-12">Core Capabilities</h2>

          <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
            {[
              { img: service1, title: "Custom Software", desc: "Tailored applications built to your needs." },
              { img: service2, title: "Backend Systems", desc: "Scalable server architecture." },
              { img: service3, title: "API Integration", desc: "Seamless system connections." },
              { img: service4, title: "Maintenance", desc: "Ongoing support & optimization." }
            ].map((service, i) => (
              <div key={i} className="p-6 border rounded-xl">
                <div className="h-40 mb-4 overflow-hidden rounded-lg">
                  <img src={service.img} className="w-full h-full object-cover" />
                </div>

                <h3 className="text-xl font-bold mb-2">{service.title}</h3>
                <p className="text-sm text-muted-foreground mb-4">{service.desc}</p>

                <Link href="/services" className="text-primary text-sm flex items-center">
                  Learn more <ChevronRight className="ml-1 w-4 h-4" />
                </Link>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Why Choose Us */}
      <section className="py-24 bg-primary text-white">
        <div className="container mx-auto px-4 grid lg:grid-cols-2 gap-12">

          <div>
            <h2 className="text-4xl font-bold mb-6">Engineering excellence.</h2>
            <p className="mb-6">
              Clean, maintainable code that solves real problems.
            </p>

            <ul className="space-y-3">
              {[
                "Predictable delivery",
                "Transparent communication",
                "Rigorous testing",
                "Security-first"
              ].map((item, i) => (
                <li key={i} className="flex items-center gap-2">
                  <CheckCircle2 /> {item}
                </li>
              ))}
            </ul>
          </div>

          <div className="rounded-xl overflow-hidden">
            <img src={heroImg} className="w-full h-full object-cover" />
          </div>

        </div>
      </section>

      {/* Technologies */}
      <section className="py-24 text-center">
        <h2 className="text-3xl font-bold mb-12">Technologies We Use</h2>

        <div className="flex flex-wrap justify-center gap-4">
          {[
            { icon: <SiPython />, label: "Python" },
            { icon: <SiDjango />, label: "Django" },
            { icon: <SiFlask />, label: "Flask" },
            { icon: <SiJavascript />, label: "JavaScript" },
            { icon: <SiReact />, label: "React" },
            { icon: <SiPostgresql />, label: "PostgreSQL" },
            { icon: <Globe />, label: "REST APIs" },
            { icon: <SiGithub />, label: "GitHub" },
            { icon: <Cloud />, label: "Cloud" },
            { icon: <Shield />, label: "Security" }
          ].map((tech, i) => (
            <div key={i} className="px-6 py-3 border rounded-lg flex items-center gap-2">
              {tech.icon}
              <span>{tech.label}</span>
            </div>
          ))}
        </div>
      </section>

      {/* Products */}
      <section className="py-24">
        <div className="container mx-auto px-4 grid md:grid-cols-2 gap-8">

          {[product1, product2].map((img, i) => (
            <div key={i} className="border rounded-xl overflow-hidden">
              <div className="h-48">
                <img src={img} className="w-full h-full object-cover" />
              </div>

              <div className="p-6">
                <h3 className="text-xl font-bold mb-2">
                  {i === 0 ? "Agribusiness Platform" : "School Management System"}
                </h3>
                <p className="text-sm text-muted-foreground">
                  Coming soon platform.
                </p>
              </div>
            </div>
          ))}

        </div>
      </section>

      {/* CTA */}
      <section className="py-24 text-center">
        <h2 className="text-4xl font-bold mb-6">
          Ready to scale your operations?
        </h2>
        <Button asChild>
          <Link href="/contact">Get in Touch</Link>
        </Button>
      </section>

    </div>
  );
}